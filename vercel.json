{
  "version": 2,
  "buildCommand": "npx prisma generate && npm run vercel-build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ]
}
